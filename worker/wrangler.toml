name = "zest-feedback-handler"
main = "feedback-handler.js"
compatibility_date = "2024-12-30"

# Deploy to custom domain route
routes = [
  { pattern = "zestcli.com/api/feedback", zone_name = "zestcli.com" }
]

# KV namespace for rate limiting
kv_namespaces = [
  { binding = "RATE_LIMIT_KV", id = "fb7712c0bb254c66860a5af297d623bf" }
]

# Environment variables (non-sensitive)
# Secrets should be added via: wrangler secret put <KEY_NAME>
[vars]
TO_EMAIL = "info@zestcli.com"
FROM_EMAIL = "info@zestcli.com"

# Add these secrets via command line:
# wrangler secret put RESEND_API_KEY
